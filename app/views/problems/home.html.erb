<div class="text-center mb-4">
  <h1>秋の漢字クイズへようこそ!</h1>
</div>

<div class="container mt-6">
  <h2>どれを選ぶ?</h2>
  <div class="row">
    <% [["易しい", "easy", "success"],
        ["難しい", "hard", "danger"],
        ["全て", nil, "primary"]].each do |title, diff, color| %>
      <div class="col-md-4 mb-3">
        <div class="card text-center">
          <div class="card-body">
            <h4 class="card-title"><%= title %></h4>
            <div class="d-flex justify-content-center gap-5">
              <%= link_to "3問", problems_path(difficulty: diff, count: 3),
                          class: "btn btn-#{color}" %>
              <%= link_to "5問", problems_path(difficulty: diff, count: 5),
                          class: "btn btn-#{color}" %>
              <%= link_to "10問", problems_path(difficulty: diff, count: 10),
                          class: "btn btn-#{color}" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
